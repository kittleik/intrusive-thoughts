# Personal config — NEVER commit (contains API keys, names, etc)
config.json

# Runtime data (generated during operation)
today_mood.json
today_schedule.json
human_mood.json
history.json
mood_history.json
streaks.json
achievements_earned.json
mood_history_backup.json
log/picks.log
journal/*.md

# Runtime data directories (created by setup.sh)
health/
memory_store/
wal/
buffer/
evolution/
trust_store/

# Keep directory structure
!log/.gitkeep
!journal/.gitkeep

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
Thumbs.db
